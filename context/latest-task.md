## api repo only

Task 1: requested changes to API endpoint sync-product-to-channel

### Constraints

- support for flags in body: isIncrementalSync optional boolean, selectedChanges optional -> create a specific DTO for body

- At any point if there is a strong need for single or multiple clarifications then you must ask for it rather than implementing purely on assumptions